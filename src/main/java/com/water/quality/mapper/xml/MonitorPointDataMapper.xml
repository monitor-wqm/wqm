<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.water.quality.mapper.MonitorPointDataMapper">

    <select id="selectPollutionWithTimesDesc" resultType="com.water.quality.pojo.dto.MonPointDataPollutionDto">
        select id, single_is_pollution pollutioned
        from monitor_point_data
        where monitor_point_id = #{monPointId}
        and is_checked = 0
        and is_handled = 0
        and pollution_record_id is null
        order by create_time DESC
        limit #{count}
    </select>
</mapper>
